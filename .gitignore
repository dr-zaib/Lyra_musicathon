# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
.env

# Node / Next (web/ has its own .gitignore too)
node_modules/
.next/
.env.local

# local tooling + scratch artifacts (preview config, headless screenshots, mockups, logs)
.claude/
web/_*.png
web/_*.html
web/*.log
